PARSER_MODE=gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.8-flash
SUPABASE_PUBLISHABLE_KEYS=
SUPABASE_SECRET_KEYS=
ELEVENLABS_API_KEY=
SUPABASE_URL=
# Supabase: project URL plus the current publishable (sb_publishable_) and secret (sb_secret_) API keys.
# Never the legacy anon/service_role JWTs. CATALOG_SOURCE=bundled serves the in-repo catalog, labelled.
CATALOG_SOURCE=supabase
ORDER_PERSISTENCE=supabase
WAIT_TIME_MODE=seeded
